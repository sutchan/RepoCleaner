# RepoCleaner 开放规格说明书 (OpenSpec)

## 1. 项目概述

### 项目名称
RepoCleaner - GitHub 项目清洁助手

### 项目类型
Windows 批处理脚本工具 (Batch Script)

### 核心功能
一键管理前端项目目录，自动清理冗余缓存文件，统一管理全局 node_modules，节省磁盘空间。

### 目标用户
- 前端开发者
- 全栈开发者
- 需要管理多个 Node.js 项目的团队

### 支持项目类型
- NextJS 项目
- React 项目
- Vue 项目
- Vite 项目
- 常规 Node.js 项目

---

## 2. 重构改进

### 2.1 模块化架构

重构后的代码采用清晰的模块化结构：

| 模块名称 | 行数范围（约） | 功能描述 |
|---------|---------|----------|
| 配置/语言加载 | 7-33 | 读取 config.ini / lang.ini，加载语言 |
| 语言变量定义 | 34-182 | 中英文双语文案变量（含 F1 二次确认文案） |
| 参数解析 | 190-266 | 命令行参数解析和处理（-lang/-h/数字/路径） |
| 目录选择 | 270-345 | 语言选择、路径选择与配置保存 |
| 菜单 | 347-390 | 主菜单显示和用户交互 |
| 功能实现 | 385-640 | 7个核心功能的实现 |

### 2.2 代码优化

#### 消除重复代码
- **问题**: 原始代码中 `ARG_RUN_1` 到 `ARG_RUN_7` 逻辑完全重复
- **解决**: 统一使用 `ARG_MODE` 变量和 `RUN_ARG_FUNCTION` 标签
- **效果**: 代码行数减少约100行

#### 参数解析优化
- **问题**: 使用大量 if 判断处理命令行参数
- **解决**: 使用变量存储和集中判断
- **效果**: 代码更清晰，易于维护

#### 成功消息处理
- **问题**: 之前使用通用 `SHOW_SUCCESS` 函数导致消息显示错误
- **解决**: 每个功能直接显示自己的成功消息
- **效果**: 消息显示准确，用户体验更好

### 2.3 新增功能

#### PATH_INPUT 变量
- **功能**: 支持交互式路径输入提示
- **中文**: "请输入完整路径: "
- **英文**: "Enter full path: "

#### HELP_* 变量组
- **功能**: 标准化帮助信息显示
- **支持**: 中文/英文双语

---

## 3. 功能规格

### 3.1 功能列表

#### 功能1: 启用全局 node_modules
- **描述**: 配置系统环境变量 NODE_PATH，禁用所有项目的本地 node_modules 生成
- **操作步骤**:
  1. 显示警告并要求二次确认（输入 Y 继续，否则返回菜单）
  2. 获取全局 npm 模块路径 (`npm root -g`)
  3. 设置 NODE_PATH 环境变量
  4. 为所有包含 package.json 的项目生成 .npmrc 配置文件
  5. 清理现有的本地 node_modules 目录
- **.npmrc 配置内容**:
  - `prefix`: 全局模块路径
  - `cache`: 缓存路径
  - `tmp`: 临时目录路径（已添加引号保护）
  - `global=true`
  - `prefer-global=true`
- **预期结果**: 项目共享全局依赖，不再生成 node_modules 文件夹

#### 功能2: 重置 Node 配置
- **描述**: 恢复系统原始状态
- **操作步骤**:
  1. 删除 NODE_PATH 环境变量
  2. 删除所有项目的 .npmrc 配置文件
- **预期结果**: npm 恢复默认行为

#### 功能3: 清理并禁用 .next 缓存
- **描述**: 禁用 NextJS 缓存功能
- **操作步骤**:
  1. 扫描所有包含 next.config.js / next.config.ts 的项目（支持 TS 配置）
  2. 删除现有的 .next 缓存目录
  3. 创建不可见的 .next 文件阻止缓存生成
  4. 设置文件属性为隐藏、系统、只读
- **预期结果**: 节省磁盘空间，但会影响 NextJS 热更新速度

#### 功能4: 恢复 NextJS 缓存
- **描述**: 恢复正常编译
- **操作步骤**:
  1. 扫描所有包含 next.config.js / next.config.ts 的项目（支持 TS 配置）
  2. 删除禁止缓存的 .next 文件
  3. 恢复文件属性
  4. 清理 .next 目录
- **预期结果**: NextJS 恢复正常编译和热更新

#### 功能5: 安装常用全局依赖
- **描述**: 一次性安装常用开发库
- **安装列表**:
  - React 生态: react, react-dom, next
  - Vue 生态: vue, @vue/cli
  - HTTP 库: axios, express
  - 包管理器: pnpm, yarn
  - 开发工具: cross-env, dotenv, nodemon, pm2, rimraf, mkdirp
- **预期结果**: 所有依赖安装到全局，项目可直接使用

#### 功能6: 清理所有项目缓存
- **描述**: 一键清理所有项目的缓存文件
- **清理项**:
  - `node_modules`: 项目依赖目录
  - `.next`: NextJS 缓存
  - `.nuxt`: NuxtJS 缓存
  - `dist`: 打包输出目录
  - `build`: 构建输出目录
  - `package-lock.json`: npm 锁文件
  - `yarn.lock`: yarn 锁文件
  - `pnpm-lock.yaml`: pnpm 锁文件
- **预期结果**: 释放磁盘空间

#### 功能7: 清理 Vite/构建缓存
- **描述**: 清理 Vite 和构建工具的缓存
- **清理项**:
  - `.vite`: Vite 缓存目录
  - `.turbo`: Turborepo 缓存
  - `.nuxt`: NuxtJS 缓存
  - `dist`: 打包输出目录
  - `build`: 构建输出目录
  - `coverage`: 测试覆盖率报告
  - `.nyc_output`: NYC 输出目录
- **预期结果**: 清理构建缓存，节省空间

### 3.2 用户交互功能

#### 语言切换
- **支持语言**: 中文 (zh)、英文 (en)
- **切换方式**:
  - 交互式菜单按 [L] 键
  - 命令行参数 `-lang en|zh`
  - 配置文件 `config.ini` 设置 `LANG=`
- **默认语言**: 中文 (zh)

#### 目录选择
- **选择方式**:
  - 命令行参数指定路径
  - 配置文件 `config.ini` 设置 `REPO_ROOT=`
  - 交互式菜单选择:
    - 使用当前目录
    - 使用上级目录
    - 手动输入路径
    - 使用默认目录
- **默认目录**: `E:\Github`

### 3.3 命令行参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `-lang en\|zh` | 设置语言 | `RepoCleaner.bat -lang en` |
| `-h` / `--help` | 显示帮助信息 | `RepoCleaner.bat -h` |
| `1-7` | 直接运行对应功能 | `RepoCleaner.bat 1` |
| 路径 | 指定仓库根目录 | `RepoCleaner.bat E:\Projects` |

---

## 4. 技术规格

### 4.1 环境要求
- **操作系统**: Windows
- **运行环境**: Windows 命令提示符 (cmd)
- **依赖软件**: Node.js 和 npm

### 4.2 文件结构
```
/workspace/
├── RepoCleaner.bat      # 主程序脚本（约630行）
├── config.ini           # 配置文件（REPO_ROOT / LANG）
├── lang.ini            # 语言配置文件（LANG 值 + [en]/[zh] 文案节）
├── README.MD           # 项目说明文档
├── CHANGELOG.MD        # 更新日志
├── OPENSPEC.MD         # 开放规格说明书
├── RepoCleaner.code-workspace  # VS Code 工作区配置
└── prototype/          # Next.js 参考界面（详见 README「原型」）
```

### 4.3 代码统计

| 指标 | 原始值 | 重构后 | 改善 |
|------|--------|--------|------|
| 总行数 | 675 | ~630 | 参数/循环精简 |
| 功能标签数 | 7个重复的ARG_RUN | 1个通用的RUN_ARG_FUNCTION | 减少约100行 |
| 语言变量定义 | 2处重复 | 统一管理 | 更易维护 |
| 帮助信息 | 硬编码 | 变量化 | 支持多语言 |

### 4.4 配置文件格式

#### config.ini
```ini
REPO_ROOT=E:\Github
LANG=zh
```

#### lang.ini
- `LANG=` 存储当前语言选择（脚本读取）
- `[en]` / `[zh]` 两个节保存中英文文案参考（脚本内置文案变量，此处供文档/后续外部化使用）
- 注意：`-lang` 参数仅写入 `config.ini`，不会覆盖 lang.ini，避免清空文案节

### 4.5 编码要求
- 文件编码: UTF-8
- 控制台编码: GBK/UTF-8 (通过 `chcp 65001` 设置)

---

## 5. 用户界面规格

### 5.1 界面风格
- 使用 ASCII 艺术边框
- 中文/英文双语支持
- 清晰的操作步骤提示
- 进度显示

### 5.2 菜单结构
```
======================================================================
               RepoCleaner - 项目清洁助手
======================================================================

            目标目录: E:\Github

         [1] 启用全局 node_modules     - 无需本地 node_modules
         [2] 重置 Node 配置            - 恢复默认设置

         [3] 清理 .next 缓存           - 禁用 NextJS 缓存
         [4] 恢复 NextJS 缓存          - 启用正常编译

         [5] 安装全局依赖              - React/Vue/Next/axios/工具
         [6] 清理所有项目缓存          - 删除 node_modules/.next/dist
         [7] 清理 Vite/构建缓存        - 删除 .vite/dist/.turbo

         [L] English / 中文
         [0] 退出

======================================================================
```

### 5.3 错误处理
- 路径不存在时显示错误信息并要求重新输入
- 操作完成后显示成功提示
- 删除操作前显示警告信息

---

## 6. 数据规格

### 6.1 环境变量
| 变量名 | 说明 | 来源 |
|--------|------|------|
| NODE_PATH | 全局模块路径 | 功能1设置 |
| TEMP | 临时目录 | 系统环境变量 |

### 6.2 生成的配置文件
| 文件名 | 位置 | 用途 |
|--------|------|------|
| .npmrc | 每个项目目录 | 配置 npm 使用全局模块 |
| .next | NextJS 项目目录 | 阻止缓存生成 |

---

## 7. 兼容性规格

### 7.1 路径处理
- 支持带空格的路径
- 支持相对路径和绝对路径
- 自动处理末尾反斜杠

### 7.2 文件属性
- .next 文件设置属性: 隐藏(H) + 系统(S) + 只读(R)
- 恢复时清除所有属性

---

## 8. 版本历史

### v1.2.2
#### 修复
- **空格路径支持**：所有 `for /d` 目录扫描改为引号包裹，真正支持含空格的仓库根目录（对齐 7.1）
- **next.config.ts 检测**：功能3/4 同时识别 `next.config.js` 与 `next.config.ts`（Next.js 15+/16 默认）
- **功能1 二次确认**：执行删除前要求输入 Y 确认，避免误删 node_modules
- **config.ini 写入修复**：`-lang` 参数与路径保存不再产生重复 `LANG=` 行、不再丢失另一配置项
- **lang.ini 保护**：`-lang` 不再整文件覆盖 lang.ini，避免清空 `[en]/[zh]` 文案节

### v1.1.0 (重构版本)
#### 重构改进
- **模块化架构**: 将代码拆分为6个清晰的模块
- **消除重复代码**: ARG_RUN_1~7 统一为 ARG_MODE
- **参数解析优化**: 使用变量存储和集中判断
- **成功消息修复**: 每个功能显示自己的成功消息
- **新增PATH_INPUT**: 支持交互式路径输入提示
- **帮助信息变量化**: 支持多语言帮助显示
- **临时目录引号保护**: tmp="!TEMP!" 防止空格问题

### v1.0.1
- 多语言支持
- 功能7: 清理 Vite/构建缓存
- 智能目录选择
- 自动保存配置
- 功能1增强：清理现有 node_modules
- .npmrc 配置优化

### v1.0.0 (初始版本)
- 基本功能实现
- 功能1-6完成

---

## 9. 测试验证

### 9.1 功能测试清单

| 功能号 | 功能名称 | 测试状态 |
|--------|----------|----------|
| 1 | 启用全局 node_modules | ✅ 通过 |
| 2 | 重置 Node 配置 | ✅ 通过 |
| 3 | 清理 .next 缓存 | ✅ 通过 |
| 4 | 恢复 NextJS 缓存 | ✅ 通过 |
| 5 | 安装全局依赖 | ✅ 通过 |
| 6 | 清理所有项目缓存 | ✅ 通过 |
| 7 | 清理 Vite/构建缓存 | ✅ 通过 |

### 9.2 交互测试

| 测试项 | 测试状态 |
|--------|----------|
| 语言切换 (zh/en) | ✅ 通过 |
| 路径选择 | ✅ 通过 |
| 命令行参数 | ✅ 通过 |
| 帮助信息显示 | ✅ 通过 |
| 配置保存/加载 | ✅ 通过 |

---

## 10. 扩展计划

### 计划功能
- [ ] 支持更多框架缓存清理 (Angular, Svelte)
- [ ] 添加 dry-run 模式预览要删除的文件
- [ ] 支持自定义清理规则
- [ ] 添加日志记录功能
- [ ] 支持恢复误删的文件
- [ ] 添加详细进度显示
- [ ] 支持批量操作确认

---

## 11. 许可证

MIT License
