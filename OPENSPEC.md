# RepoCleaner 开放规格说明书 (OpenSpec)

## 1. 项目概述

### 项目名称
RepoCleaner - GitHub 项目清洁助手

### 项目类型
Windows 批处理脚本工具 (Batch Script)

### 核心功能
一键管理前端项目目录，自动清理冗余缓存文件，统一管理全局 node_modules，节省磁盘空间。

### 目标用户
- 前端开发者
- 全栈开发者
- 需要管理多个 Node.js 项目的团队

### 支持项目类型
- NextJS 项目
- React 项目
- Vue 项目
- Vite 项目
- 常规 Node.js 项目

---

## 2. 功能规格

### 2.1 功能列表

#### 功能1: 启用全局 node_modules
- **描述**: 配置系统环境变量 NODE_PATH，禁用所有项目的本地 node_modules 生成
- **操作步骤**:
  1. 获取全局 npm 模块路径 (`npm root -g`)
  2. 设置 NODE_PATH 环境变量
  3. 为所有包含 package.json 的项目生成 .npmrc 配置文件
  4. 清理现有的本地 node_modules 目录
- **.npmrc 配置内容**:
  - `prefix`: 全局模块路径
  - `cache`: 缓存路径
  - `tmp`: 临时目录路径
  - `global=true`
  - `prefer-global=true`
- **预期结果**: 项目共享全局依赖，不再生成 node_modules 文件夹

#### 功能2: 重置 Node 配置
- **描述**: 恢复系统原始状态
- **操作步骤**:
  1. 删除 NODE_PATH 环境变量
  2. 删除所有项目的 .npmrc 配置文件
- **预期结果**: npm 恢复默认行为

#### 功能3: 清理并禁用 .next 缓存
- **描述**: 禁用 NextJS 缓存功能
- **操作步骤**:
  1. 扫描所有包含 next.config.js 的项目
  2. 删除现有的 .next 缓存目录
  3. 创建不可见的 .next 文件阻止缓存生成
  4. 设置文件属性为隐藏、系统、只读
- **预期结果**: 节省磁盘空间，但会影响 NextJS 热更新速度

#### 功能4: 恢复 NextJS 缓存
- **描述**: 恢复正常编译
- **操作步骤**:
  1. 删除禁止缓存的 .next 文件
  2. 恢复文件属性
  3. 清理 .next 目录
- **预期结果**: NextJS 恢复正常编译和热更新

#### 功能5: 安装常用全局依赖
- **描述**: 一次性安装常用开发库
- **安装列表**:
  - React 生态: react, react-dom, next
  - Vue 生态: vue, @vue/cli
  - HTTP 库: axios, express
  - 包管理器: pnpm, yarn
  - 开发工具: cross-env, dotenv, nodemon, pm2, rimraf, mkdirp
- **预期结果**: 所有依赖安装到全局，项目可直接使用

#### 功能6: 清理所有项目缓存
- **描述**: 一键清理所有项目的缓存文件
- **清理项**:
  - `node_modules`: 项目依赖目录
  - `.next`: NextJS 缓存
  - `.nuxt`: NuxtJS 缓存
  - `dist`: 打包输出目录
  - `build`: 构建输出目录
  - `package-lock.json`: npm 锁文件
  - `yarn.lock`: yarn 锁文件
  - `pnpm-lock.yaml`: pnpm 锁文件
- **预期结果**: 释放磁盘空间

#### 功能7: 清理 Vite/构建缓存
- **描述**: 清理 Vite 和构建工具的缓存
- **清理项**:
  - `.vite`: Vite 缓存目录
  - `.turbo`: Turborepo 缓存
  - `.nuxt`: NuxtJS 缓存
  - `dist`: 打包输出目录
  - `build`: 构建输出目录
  - `coverage`: 测试覆盖率报告
  - `.nyc_output`: NYC 输出目录
- **预期结果**: 清理构建缓存，节省空间

### 2.2 用户交互功能

#### 语言切换
- **支持语言**: 中文 (zh)、英文 (en)
- **切换方式**:
  - 交互式菜单按 [L] 键
  - 命令行参数 `-lang en|zh`
  - 配置文件 `config.ini` 设置 `LANG=`
- **默认语言**: 中文 (zh)

#### 目录选择
- **选择方式**:
  - 命令行参数指定路径
  - 配置文件 `config.ini` 设置 `REPO_ROOT=`
  - 交互式菜单选择:
    - 使用当前目录
    - 使用上级目录
    - 手动输入路径
    - 使用默认目录
- **默认目录**: `E:\Github`

### 2.3 命令行参数

| 参数 | 说明 | 示例 |
|------|------|------|
| `-lang en\|zh` | 设置语言 | `RepoCleaner.bat -lang en` |
| `-h` / `--help` | 显示帮助信息 | `RepoCleaner.bat -h` |
| `1-7` | 直接运行对应功能 | `RepoCleaner.bat 1` |
| 路径 | 指定仓库根目录 | `RepoCleaner.bat E:\Projects` |

---

## 3. 技术规格

### 3.1 环境要求
- **操作系统**: Windows
- **运行环境**: Windows 命令提示符 (cmd)
- **依赖软件**: Node.js 和 npm

### 3.2 文件结构
```
/workspace/
├── RepoCleaner.bat      # 主程序脚本
├── config.ini           # 配置文件
├── lang.ini            # 语言配置文件
├── README.MD           # 项目说明文档
├── CHANGELOG.MD        # 更新日志
└── RepoCleaner.code-workspace  # VS Code 工作区配置
```

### 3.3 配置文件格式

#### config.ini
```ini
REPO_ROOT=E:\Github
LANG=zh
```

#### lang.ini
- 包含中英文双语配置
- 分 [en] 和 [zh] 两个节

### 3.4 编码要求
- 文件编码: UTF-8
- 控制台编码: GBK/UTF-8 (通过 `chcp 65001` 设置)

---

## 4. 用户界面规格

### 4.1 界面风格
- 使用 ASCII 艺术边框
- 中文/英文双语支持
- 清晰的操作步骤提示
- 进度显示

### 4.2 菜单结构
```
======================================================================
               RepoCleaner - 项目清理工具
======================================================================

            目标目录: E:\Github

         [1] 启用全局 node_modules     - 无需本地 node_modules
         [2] 重置 Node 配置            - 恢复默认设置

         [3] 清理 .next 缓存           - 禁用 NextJS 缓存
         [4] 恢复 NextJS 缓存          - 启用正常编译

         [5] 安装全局依赖              - React/Vue/Next/axios/工具
         [6] 清理所有项目缓存          - 删除 node_modules/.next/dist
         [7] 清理 Vite/构建缓存        - 删除 .vite/dist/.turbo

         [L] English / 中文
         [0] 退出

======================================================================
```

### 4.3 错误处理
- 路径不存在时显示错误信息并要求重新输入
- 操作完成后显示成功提示
- 删除操作前显示警告信息

---

## 5. 数据规格

### 5.1 环境变量
| 变量名 | 说明 | 来源 |
|--------|------|------|
| NODE_PATH | 全局模块路径 | 功能1设置 |
| TEMP | 临时目录 | 系统环境变量 |

### 5.2 生成的配置文件
| 文件名 | 位置 | 用途 |
|--------|------|------|
| .npmrc | 每个项目目录 | 配置 npm 使用全局模块 |
| .next | NextJS 项目目录 | 阻止缓存生成 |

---

## 6. 兼容性规格

### 6.1 路径处理
- 支持带空格的路径
- 支持相对路径和绝对路径
- 自动处理末尾反斜杠

### 6.2 文件属性
- .next 文件设置属性: 隐藏(H) + 系统(S) + 只读(R)
- 恢复时清除所有属性

---

## 7. 版本历史

### v1.0.1 (当前版本)
- 多语言支持
- 功能7: 清理 Vite/构建缓存
- 智能目录选择
- 自动保存配置
- 功能1增强：清理现有 node_modules
- .npmrc 配置优化

### v1.0.0 (初始版本)
- 基本功能实现
- 功能1-6完成

---

## 8. 已知问题和修复记录

### 修复的问题

#### 问题1: 帮助信息硬编码
- **状态**: 已修复
- **描述**: :SHOW_HELP 标签使用硬编码英文，未使用语言变量
- **修复**: 添加语言判断，根据 LANG_SECTION 显示对应语言

#### 问题2: 临时目录路径未加引号
- **状态**: 已修复
- **描述**: `!TEMP!` 变量未用引号包围，可能导致路径包含空格时出现问题
- **修复**: 修改为 `tmp="!TEMP!"`

---

## 9. 扩展计划

### 计划功能
- [ ] 支持更多框架缓存清理 (Angular, Svelte)
- [ ] 添加 dry-run 模式预览要删除的文件
- [ ] 支持自定义清理规则
- [ ] 添加日志记录功能
- [ ] 支持恢复误删的文件

---

## 10. 许可证

MIT License
